@{
    ViewData["Title"] = "Test Suspended";
    Layout = null; // Kill structural master navigation dashboard headers safely to freeze workspace visibility options
}

<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <title>Exam Terminated</title>
</head>
<body class="bg-light">
    <div class="container mt-5 pt-5 text-center">
        <div class="card shadow border-danger mx-auto" style="max-width: 600px;">
            <div class="card-header bg-danger text-white p-4">
                <h1 class="display-6">🚫 ASSESSMENT ACCESS TERMINATED</h1>
            </div>
            <div class="card-body p-5">
                <p class="lead text-dark font-weight-bold">Your exam profile session execution scope was instantly halted by automatic systems protection engine parameters rules filters.</p>
                <div class="alert alert-secondary font-monospace my-4">Reason: Maximum Tab Window context switches limits exceeded criteria limit bounds (3/3 threshold breaches)</div>
                <p class="text-muted">The precise telemetry monitoring metrics log indices values (copy attempts counts, visibility shifts events, external secondary track behaviors tracking values) have been permanently stored into database servers records grid context next to student identity flags parameters tracking schemas logs.</p>
                <hr />
                <p class="text-danger small font-italic font-weight-bold">Contact institutional server network administrators or proctor supervisor board to dispute security records flags parameters tracking indexes values.</p>
            </div>
        </div>
    </div>
</body>
</html>